# 商品发布页筛选功能说明

## 功能概述

已为发布页的三个商品列表添加筛选功能，支持按多个维度快速过滤商品。

## 三个列表的筛选功能

### 1. 采集箱待编辑清单

**筛选维度：**
- 店铺：下拉选择特定店铺
- 站点：下拉选择特定站点
- 来源平台：下拉选择来源（1688/拼多多/Temu/亚马逊）

**位置：** 采集箱清单卡片内，批量操作条上方

### 2. 未认领待认领清单

**筛选维度：**
- 来源平台：下拉选择来源平台
- 价格区间：最低价和最高价输入框（美元）

**位置：** 未认领清单卡片内，批量操作条上方

### 3. 数据搬家清单

**筛选维度：**
- 来源平台：下拉选择来源平台
- 源站点：下拉选择源站点
- 价格区间：最低价和最高价输入框（美元）

**位置：** 数据搬家清单卡片内，批量操作条上方

## 使用方式

1. **动态选项**：筛选下拉框的选项从当前扫描到的数据中自动提取，只显示实际存在的值
2. **实时过滤**：选择筛选条件后立即生效，无需点击确认
3. **组合筛选**：可以同时使用多个筛选条件，结果为交集
4. **清除筛选**：点击「清除筛选」按钮一键重置所有筛选条件
5. **与现有过滤共存**：
   - 采集箱的「只看未编辑」开关仍然有效
   - 未认领的「只看可认领」开关仍然有效
   - 数据搬家的「隐藏已搬本店」开关仍然有效
   - 筛选在这些开关过滤的基础上再次过滤

## 技术实现

### 前端改动

1. **HTML 结构**：在每个列表的批量操作条上方添加筛选控件
2. **JavaScript 变量**：
   - `scanFilters`：采集箱筛选条件
   - `crawlFilters`：未认领筛选条件
   - `banjiaFilters`：数据搬家筛选条件
3. **过滤逻辑**：修改 `visibleXxxItems()` 函数，在原有过滤基础上叠加筛选条件
4. **动态选项**：新增 `updateXxxFilterOptions()` 函数，从数据中提取唯一值填充下拉框

### 关键函数

- `visibleScanItems()`：采集箱可见行计算（编辑进度过滤 + 筛选）
- `visibleCrawlItems()`：未认领可见行计算（可否认领过滤 + 筛选）
- `visibleBanjiaItems()`：数据搬家可见行计算（已搬本店过滤 + 筛选）
- `updateScanFilterOptions()`：更新采集箱筛选下拉选项
- `updateCrawlFilterOptions()`：更新未认领筛选下拉选项
- `updateBanjiaFilterOptions()`：更新数据搬家筛选下拉选项

## 验证要点

1. ✓ 筛选变量已定义（3 个）
2. ✓ 筛选控件已添加到 HTML（13 个元素）
3. ✓ 筛选逻辑已集成到可见行计算函数
4. ✓ 筛选选项更新函数已添加（3 个函数，共 9 处调用）
5. ✓ 清除筛选按钮已绑定事件（3 个按钮）
6. ✓ 筛选下拉和输入框已绑定 change/input 事件

## 注意事项

1. **价格筛选基于美元**：未认领和数据搬家的价格筛选使用 `priceUsd` 字段
2. **站点名归一化**：数据搬家的源站点筛选使用 `normSite()` 函数去掉尾部的「站」字
3. **勾选保持**：筛选后被隐藏的行不会自动取消勾选（与现有「只看未编辑」逻辑一致）
4. **全选作用范围**：「全选本页」只勾选当前筛选后显示的行
5. **计数准确性**：「已选 N 条」计数只统计当前可见且已勾选的行

## 测试建议

启动服务后在浏览器中测试：

```powershell
cd C:\Users\Administrator\Desktop\manus-gui
python app.py
```

访问 http://localhost:8000/publish

1. 点击各列表的「立即扫描」获取数据
2. 测试各个筛选下拉框是否有选项
3. 选择不同筛选条件观察列表变化
4. 测试价格区间筛选（输入最低价/最高价）
5. 测试组合筛选（同时使用多个条件）
6. 测试「清除筛选」按钮
7. 测试筛选与原有开关（只看未编辑/只看可认领/隐藏已搬本店）的配合
