pydantic~=2.10.6
openai~=1.66.3
tenacity~=9.0.0
pyyaml~=6.0.2
loguru~=0.7.3
numpy
datasets~=3.4.1
fastapi~=0.115.11
tiktoken~=0.9.0

html2text~=2024.2.26
gymnasium~=1.1.1
pillow~=10.4.0
browsergym~=0.13.3
uvicorn~=0.34.0
unidiff~=0.7.5
browser-use~=0.1.40
googlesearch-python~=1.3.0
baidusearch~=1.0.3
duckduckgo_search~=7.5.3

aiofiles~=24.1.0
pydantic_core~=2.27.2
colorama~=0.4.6
playwright~=1.51.0

docker~=7.1.0
pytest~=8.3.5
pytest-asyncio~=0.25.3

mcp~=1.5.0
httpx>=0.27.0
tomli>=2.0.0

boto3~=1.37.18

requests~=2.32.3
beautifulsoup4~=4.13.3
crawl4ai~=0.6.3

huggingface-hub~=0.29.2
setuptools~=75.8.0
structlog
daytona-sdk

# RAG 经验库（成功流程检索 + few-shot 注入）
faiss-cpu~=1.8.0
rank-bm25~=0.2.2
jieba~=0.42.1

# 系统桌面级 computer_use 工具（整屏截图 + 鼠标/键盘控制 + 窗口置前）
# 连带装 pygetwindow（focus_window 用）；中文输入走剪贴板时需 pyperclip
pyautogui~=0.9.54
pyperclip~=1.8.2

# 产品视频合规化（发布阶段⑥b：把 1688 搬来的 9:16 竖屏裁成 Temu 只收的 1:1/3:4/16:9）
# 自带 win_amd64 的 ffmpeg.exe，不必装系统 ffmpeg（这台机器 PATH 里没有）；
# 注意它【不带 ffprobe】，故 video.probe_video 要能只靠 ffmpeg 读元数据
imageio-ffmpeg~=0.6.0

# 补全：项目实际 import 但历史 requirements.txt 遗漏的顶级包（2026-09-03 重建 venv 时发现）
# markdownify：浏览器 DOM → Markdown（browser_use_tool 提取页面内容）
# openpyxl/pandas：Excel 读写与表格数据处理（orders pipeline / 图表工具）；
#   注意 WPS DISPIMG 表仍走 wps_excel_tool，不能用 openpyxl 保存
# pywin32/pygetwindow：Windows 剪贴板与窗口控制（browser_use_tool 的 paste_image / focus_window）
# lxml：BeautifulSoup 的 HTML 解析后端（bing_search 用 "lxml"）
markdownify~=0.14.1
openpyxl~=3.1.5
pandas~=2.2.3
pywin32==312
pygetwindow~=0.0.9
lxml~=5.3

# 管线错误集中上报到公网 MySQL（app/error_report.py，切面 + 注解式）
# 纯 Python、Windows 无编译负担；未安装时上报静默跳过、不影响主流程
pymysql~=1.1.1
